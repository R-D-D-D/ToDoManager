
<h1>Listing tasks</h1>

<table>
  <tr>
    <th>Task</th>
    <th>Deadline</th>
    <th></th>
  </tr>

  <% @tasks.each do |task| %>
    <tr>
      <td><%= task.task %></td>
      <td><%= task.deadline %></td>
      <td><%= link_to 'Show', task_path(task) %></td>
    </tr>
  <% end %>
</table>